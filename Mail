withColumn(
    "LastResponseTime",
    when(col("LastResponseTime") == "NULL", None)
    .otherwise(col("LastResponseTime"))
    .cast("timestamp")
)
.withColumn(
    "Execution_Time",
    (unix_timestamp("LastResponseTime") - unix_timestamp("StartTime")).cast("double")
)
